# StowMan

Executable Ruby gem for managing dotfiles packages with GNU Stow.

## Development

Install dependencies:

```bash
bundle install
```

Run tests:

```bash
bundle exec rake test
```

Run executable:

```bash
bundle exec ruby exe/stow-man
```
